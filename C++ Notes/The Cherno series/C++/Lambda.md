Метод использовать «анонимные» функции.
```c++
auto lambda [](int value){cout<<"Value: “" << val <<end; }
```
`[]` – captures – как будут передаваться переменные. 
`[=]` – передавать все как переменные 
`[&]` – передавать все как ссылки
`[a]` – передать переменную а.

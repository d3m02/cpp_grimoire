+ pointers vs reference argument passing
+ variables scope
+ basic instructions (for/while ...)
+ macros
+ nline functions. Is inlining of functions marked with "inline" guaranteed?
+ Which class methods are defined implicitly?
+ volatile
+ static vs dynamic lib
+ template example. What happens when using templates
+ preprocessing vs compiling vs linking
+ cast types, RTTI
+ Most vexing parse
+ linking vs loading